---
import Base from '../layouts/Base.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import { legal, site } from '../lib/content';

const doc = legal.terms;
---

<Base
  title={doc.heading}
  description="No guarantee of rankings or citations, timelines are indicative, scope is written, and payment terms per service tier."
>
  <section class="section section--flush" style="padding-top:2.5rem">
    <div class="wrap narrow">
      <Breadcrumbs trail={[{ label: doc.heading, href: '/terms' }]} />

      <h1 style="font-size:var(--fs-3xl)">{doc.heading}</h1>
      <p class="note" style="margin-top:0.5rem">
        Last updated <time datetime={doc.updated}>{doc.updated}</time>
      </p>

      <p class="answer" style="margin-top:1.5rem">{doc.answer}</p>

      <div class="prose">
        {
          doc.sections.map((s) => (
            <>
              <h2>{s.title}</h2>
              <p>{s.body}</p>
            </>
          ))
        }

        <h2>Contact</h2>
        <p>
          Questions about these terms:
          <a href={`mailto:${site.contactEmail}`}>{site.contactEmail}</a>.
        </p>
      </div>
    </div>
  </section>
</Base>
